<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Lingvo90"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CherninBagrutProj">
        <activity
            android:name=".AboutActivity"
            android:exported="true" />
        <activity
            android:name=".CreateSentenceGroupActivity"
            android:exported="true" />
        <activity
            android:name=".SentenceTodayActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateSentenceActivity"
            android:exported="true" />
        <activity
            android:name=".SentenceActivity"
            android:exported="true" /> <!-- <meta-data -->
        <!-- android:name="com.google.android.actions" -->
        <!-- android:resource="@xml/actions" /> -->
        <activity
            android:name=".SentenceGroupsListActivity"
            android:exported="true"></activity>



        <service android:name=".PlayMusicService"/>
    </application>

</manifest>